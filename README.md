# GPU-Emulator
A CPU-based GPU emulator that implements the full SIMT execution model — warps, thread blocks, divergence, shared memory, and a complete memory hierarchy with bank conflict and coalescing analysis. Culminates in running a real GPT-2 transformer layer without any GPU hardware.
